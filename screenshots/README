# Validation Evidence

This folder contains validation screenshots supporting infrastructure configuration and failover testing.

Included Evidence:

- Replication health verification (repadmin output)
- Authentication via secondary domain controller
- Domain join during primary controller outage
- pfSense VLAN interface configuration
- DHCP relay configuration

Screenshots provide operational proof of redundancy and segmentation validation.
